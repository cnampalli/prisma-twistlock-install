---
# Dev — shared non-secret overrides for ALL Prisma host classes (console,
# scanner, sandbox). rhel_baseline knobs common to every class belong here;
# class-specific values live in prisma_{console,scanner,sandbox}.yml.
# Stub: all current baseline knobs are either class-specific or role defaults.
