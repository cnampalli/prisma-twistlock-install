---
# Requires collections: ansible.posix (firewalld). Pinned in requirements.yml.
galaxy_info:
  role_name: prisma_monitoring
  description: node_exporter + rsyslog shipping for Prisma Console
  license: internal
  min_ansible_version: "2.15"
dependencies: []
