---
# Requires collections: ansible.builtin only. Pinned base in requirements.yml.
galaxy_info:
  role_name: podman_config
  author: Chaitanya Nampalli
  description: Configure rootful podman for air-gapped FIPS Prisma Console host
  license: internal
  min_ansible_version: "2.15"
dependencies: []
