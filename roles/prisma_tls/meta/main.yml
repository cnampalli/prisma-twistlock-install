---
# Requires collections: community.crypto (x509_certificate_info,
# openssl_privatekey_info). Pinned in requirements.yml.
galaxy_info:
  role_name: prisma_tls
  author: Chaitanya Nampalli
  description: Stage internal-CA-issued TLS material for Prisma Console
  license: internal
  min_ansible_version: "2.15"
dependencies: []
