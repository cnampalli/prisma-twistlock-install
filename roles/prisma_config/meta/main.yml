---
# Requires collections: ansible.builtin only. Pinned base in requirements.yml.
galaxy_info:
  role_name: prisma_config
  author: Chaitanya Nampalli
  description: Render twistlock.cfg for FIPS-mode Console install
  license: internal
  min_ansible_version: "2.15"
dependencies: []
