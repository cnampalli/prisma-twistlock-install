---
# Requires collections: ansible.builtin only. Pinned base in requirements.yml.
galaxy_info:
  role_name: docker_config
  author: Chaitanya Nampalli
  description: Configure docker-ce daemon (daemon.json, systemd override) for scanner/sandbox hosts
  license: internal
  min_ansible_version: "2.15"
dependencies: []
