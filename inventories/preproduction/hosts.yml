---
# Preproduction inventory. Two sites: primary (live) + secondary (DR target).
# Hosts connect by DNS name (resolvable FQDN). IP addressing and the SSH
# transport are managed by the external Ansible/AAP platform team and are
# deliberately not pinned here.
#
# A host belongs to two orthogonal axes:
#   - role group: prisma_console / prisma_scanner / prisma_sandbox (under prisma_all)
#   - site group: primary / secondary  (see group_vars/{primary,secondary}.yml)
all:
  children:
    prisma_all:
      children:
        prisma_console:
        prisma_scanner:
        prisma_sandbox:
    primary:
      hosts:
        prisma-console-preprod-primary:
        prisma-scanner-preprod-primary:
        prisma-sandbox-preprod-primary:
    secondary:
      hosts:
        prisma-console-preprod-secondary:
        prisma-scanner-preprod-secondary:
        prisma-sandbox-preprod-secondary:
    prisma_console:
      hosts:
        prisma-console-preprod-primary:
        prisma-console-preprod-secondary:
    prisma_scanner:
      hosts:
        prisma-scanner-preprod-primary:
        prisma-scanner-preprod-secondary:
    prisma_sandbox:
      hosts:
        prisma-sandbox-preprod-primary:
        prisma-sandbox-preprod-secondary:
  vars:
    ansible_user: ansible
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
