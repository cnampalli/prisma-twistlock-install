---
# No external collections required. SELinux labelling uses the semanage CLI
# (policycoreutils-python-utils, installed by rhel_baseline) instead of
# community.general.sefcontext, because the air-gapped EE does not ship it.
galaxy_info:
  role_name: prisma_storage
  author: Chaitanya Nampalli
  description: Ensure the Prisma Console data folder exists; SELinux-label it for container bind-mount
  license: internal
  min_ansible_version: "2.15"
dependencies: []
